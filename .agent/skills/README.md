# Skills

Store focused workflow instructions here.

A skill should be narrow, practical, and tied to repeatable work in this
repository.

Use `core/` and generic prompts as the default baseline. Put feature-specific
guidance here when it would otherwise make the shared workflow too narrow.
