/** @deprecated Use workspace policies instead (Policy enum from workspace/model/policy.model.ts) */
export enum Role {
    admin = "admin",
    user = "user",
    automation = "automation",
    developer = "developer",
}
